{
  "scripts": {
    "build": "bun run scripts/build.ts 2>/dev/null || node scripts/build.ts"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.1.0",
    "esbuild": "^0.20.0"
  },
  "trustedDependencies": ["@biomejs/biome"]
}
